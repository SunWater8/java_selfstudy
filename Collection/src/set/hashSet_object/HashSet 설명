HashSet

set의 일종으로, 데이터의 순서가 없고, 중복된 데이터를 같은 HashSet에 넣을 수 없다.
HashSet에서 중요한 것은 객체가 동일한지를 판단하는 것이다.
프로그래머가 만든 서로 다른 두 객체들은 데이터 값들이 같더라도 중복 처리를 못하고 모두 HashSet에 저장한다.
그 안에 같은 데이터 값을 갖고 있는지를 판단하려면 두 객체가 객체 내부에 동일한 데이터를 갖고 있는지 알 수 있도록 해야 한다.
비교를 하기 위해서는 equals와 hashCode 메소드가 필요하다.

hashCode() 메소드는 두 객체가 같은 실제로 같은 내용으로 구성되었다면 같은 두 객체의 참조값도 같은 값으로 만들어주는 코드를 구현해야 한다. 
Code248 클래스와 연결된 Person 클래스의 hashCode() 메소드에서 보듯, name의 hashCode()값과 age를 더해서 두 객체가 같은 값을 반환하면 같은 객체로 보는 것이다.


